//Function Declaration
//2. Write a function to calculate the square of a number and log the result to the console.
function square(num){
    const sqrt = num * num;
    console.log(sqrt);
}
square(20);