// Task 10: Use a for loop to iterate over the array and log each element to the console.
let fruits = ["mango", "apple", "grapes", "kiwi"];
for (let i = 0; i < fruits.length; i++) {
  console.log("for loop", fruits[i]);
}

